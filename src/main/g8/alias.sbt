addCommandAlias("cover", ";jacoco:cover")

addCommandAlias("distlocal", ";dist;publishLocal")

addCommandAlias("distpub", ";dist;publish")

addCommandAlias("distsign", ";dist;publishSigned")

addCommandAlias("itest",  ";reStart;it:test;reStop")

