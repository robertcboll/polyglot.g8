package $package$.scala

import org.scalatest._

class $name;format="Camel"$IT extends FlatSpec with Matchers {

  "A $name;format="Camel"$" should "return the inverse of its input" in {
    $name;format="Camel"$.testable(true) should be (false)
  }
}
